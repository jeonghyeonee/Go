package main

import (
	"fmt"
)

func main() {
	var n int
	// Inpute the number of N (3 <= N <= 35)
	fmt.Scanln(&n)

	// fmt.Scanln()

	// reader := bufio.NewReader(os.Stdin)
	
	for i := 0; i < n; i++ {
		fmt.Scanln("%d%d", )
	}

	fmt.Println()
}